[project]
name = "geoparquet-scripts"
version = "0.1.0"
description = ""
authors = []
license = "MIT"
requires-python = ">=3.9"

dependencies = [
    black,
    click,
    geopandas,
    isort,
    jsonschema,
    pandas,
    pyarrow,
    pytest,
    shapely,
]
